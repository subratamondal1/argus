@AGENTS.md

# CLAUDE.md — Argus (Claude Code shim over the canonical AGENTS.md)

> **The canonical rules are in [`AGENTS.md`](AGENTS.md)**, imported above via `@AGENTS.md` (Claude
> Code expands it at load time, so you already have the full invariant, the pipeline, the stack,
> forbidden abstractions, code style, and commands). **EDIT [`AGENTS.md`](AGENTS.md), not this
> file** — it is the single source of truth every coding agent reads directly.
>
> If the `@AGENTS.md` import did not load (older Claude Code), **read [`AGENTS.md`](AGENTS.md) now**
> before any work in this repo.
