# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.ty_cache/

# uv / venv
.venv/
uv.lock.bak

# env / secrets — NEVER commit real keys
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# local data / traces
*.log
/data/
/traces/

# eval run artifacts (the golden set + thresholds are committed; reports are not)
/eval/last_report.json
